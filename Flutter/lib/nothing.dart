import 'package:flutter/material.dart';

// alias for sized box with width and height 0
class Nothing extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return SizedBox();
  }
}
